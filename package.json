{
  "name": "caal-app-prototype",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "connect-pg-simple": "^10.0.0"
  }
}
